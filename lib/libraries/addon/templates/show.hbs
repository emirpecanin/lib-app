{{#if @model}}
<div class="container pt-4 grid grid-cols-3 gap-4">

  {{#each @model as |library|}} 
    <LibraryContainer @library={{library}} />
  {{/each}}

</div>

{{else}}
  <Alert @color={{'red'}}>
    No libraries in database
  </Alert>
{{/if}}