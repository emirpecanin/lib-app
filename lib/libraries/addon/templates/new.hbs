<div class="flex justify-center">
  <form class="form bg-white p-6 my-10 relative w-1/3 sm:w-2/4">  
    <h3 class="text-2xl text-gray-900 font-semibold">Add new library</h3>
    <p class="text-gray-600"> Please input requested information </p>

    <Input 
      @value={{this.name}}
      type="text" 
      placeholder="Library name" 
      class="border p-2 w-full mt-3" 
    />

    <Input 
      @value={{this.address}}
      type="text" 
      placeholder="Library adress" 
      class="border p-2 w-full mt-3" 
    />

    <Input 
      @value={{this.phoneNumber}}
      type="text" 
      placeholder="Telephone number" 
      class="border p-2 w-full mt-3" 
    />

    <Textarea 
      @value={{this.additionalInfo}}
      cols="10" 
      rows="3" 
      placeholder="Additional info (optional)" 
      class="border p-2 mt-3 w-full"
      />

    <button 
      disabled={{this.isDisabled}}
      {{on 'click' this.addLibrary}}
      type="button" 
      class="{{if this.isDisabled 'disabled'}} w-full mt-6 bg-blue-600 hover:bg-blue-500 text-white font-semibold p-3"
    >
      Add new Library
    </button>
  </form>
</div>